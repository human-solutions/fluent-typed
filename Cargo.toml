[package]
name = "fluent-typed"
version = "0.1.0"
edition = "2021"

[dependencies]
fluent-syntax = "0.11"

[dev-dependencies]
unic-langid = { version = "0.9", features = ["macros"] }
fluent = "0.16"
intl-memoizer = "0.5"
